spec_version: "0.1.0"
name: tele-crypto-bot
version: 0.1.0
description: Gitagent agent for tele-crypto-bot
model:
  preferred: "openai:gpt-4o-mini"
  fallback: []
tools: [cli, read, write, memory]
runtime:
  max_turns: 50
